// import { selectSupplierProductDomain } from '../selectors';

describe('selectSupplierProductDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
