// import { selectDashboardNewDomain } from '../selectors';

describe('selectDashboardNewDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
