/*
 * DashboardNew Messages
 *
 * This contains all the text for the DashboardNew container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.DashboardNew';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the DashboardNew container!',
  },
});
