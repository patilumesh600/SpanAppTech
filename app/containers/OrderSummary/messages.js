/*
 * OrderSummary Messages
 *
 * This contains all the text for the OrderSummary container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.OrderSummary';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the OrderSummary container!',
  },
});
